In this exercise we create Threads that follow a specific workflow:

        -> action1 ->
action0 -> action2 -> action4 -> action5
        -> action3 ->

Each thread waits a fixed time and then increments a counter.